/*import {useCallback, useContext, useState} from 'react'
import { getTwinsService } from 'services/getTwins'

export function useUser() {
    const getTwins = useCallback(({id}) => {
        getTwinsService({id})
            .then()
    }, [])
}*/