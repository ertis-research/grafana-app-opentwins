export interface ExampleAppSettings {
  customText?: string;
  customCheckbox?: boolean;
}

export enum ROUTES {
  One = 'twins',
  Two = 'types',
  Three = 'agents',
  Four = 'connections',
  Five = 'policies'
}
